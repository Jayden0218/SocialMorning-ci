# Copy to apps/api/.env (gitignored). Never paste real values into chat, commits or logs.
DATABASE_URL=postgres://user:password@host/db?sslmode=require
SESSION_PEPPER=change-me-32-random-bytes-base64url
