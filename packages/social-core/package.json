{
  "name": "@socialmorning/social-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pure rules shared by the phone and the server: position merge, heat bucketing and normalisation, moment capture, sign-in lockout, comment ordering. No I/O, no node: imports (specs/002-m3-social-layer/contracts/social-core.ts).",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "tsx --test test/*.test.ts",
    "test:coverage": "c8 --100 --include 'src/**' --exclude 'src/types.ts' --exclude 'src/index.ts' --reporter=text -- tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "c8": "^10.1.3",
    "tsx": "^4.19.0",
    "typescript": "^5.9.2"
  }
}
